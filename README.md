# salonx-demo-v2

SalonX web demo (Vite + React). **This repository contains only the `salonx-web-v2` app** — not the mobile `app/` tree or other monorepo folders.

## Develop

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
