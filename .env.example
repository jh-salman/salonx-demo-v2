# Build-time: public URL of v2-admin (no trailing slash). Enables direct SSE + fastest sync.
# Example: https://admin.xsalonx.com
#
# If unset in production, the app uses `/salonx-admin` proxy from `vercel.json` (same-origin + poll).
VITE_V2_ADMIN_URL=https://admin.xsalonx.com/
